
## Requirements

* GNU Make 4.3+.

* Docker Engine and Docker Compose.

## Installation
* Copy `.env.example` as `.env` and add DB connection credentials
* Run `make setup` command to install the application.
* Run migrations & seeders if required.
* Run `make up` command to start the containers.
* Use `make cli` command to enter interactive terminal.
